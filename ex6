#include <stdio.h>

int main(void) {
  float a, b, x;
  
  printf("Escreva os coeficientes: ");
  scanf("%f %f", &a, &b);
  
  x=-b/a;
  if (a==0) { // faz a conta da raiz da equacao e indentifica se existe raiz ou nao
    printf ("raiz inexistente");
      }else{
    printf("a raiz é: %f", x);
      }

return 0;
}
