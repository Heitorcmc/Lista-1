#include <math.h>
#include <stdio.h>

int main(void) {
  float nota;

  printf("Qual foi a sua nota: ");
  scanf("%f", &nota);
  if (nota >= 8 && nota <= 10) {
    printf("Nota Ótima");
  } else if (nota >= 7 && nota < 8) {
    printf("Nota Boa");
  } else if (nota >= 5 && nota < 7) {
    printf("Nota Regular");
  } else {
    printf("Nota Insatisfatoria");
  }

  return 0;
}
