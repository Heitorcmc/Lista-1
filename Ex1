#include <stdio.h>

int main(void) {
  int x,y;
  printf("Digite dois numeros:");
  scanf("%d %d", &x,&y);
  if (x>y){  // indentifica os dios numeros e printa o maior
    printf ("%d", x);
  } else {
    printf ("%d",y);
  }
  return 0;
}
